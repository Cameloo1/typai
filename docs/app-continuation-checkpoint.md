# App Continuation Checkpoint

Status date: 2026-05-21.

Final decision: **app and Codex-adapter work may proceed**.

This checkpoint closes the release and production-asset containment pass for
the current repo state. It does not publish, mutate npm dist-tags, create Git
tags, change versions, generate production language assets, or start the next
feature phase.

## Release Lane

Lane status: **parked with app work allowed**.

Current verified state:

- npm registry: all seven public packages exist at `0.0.0-beta.0`.
- npm dist-tags: `beta` and `latest` both point at `0.0.0-beta.0` for every
  public package.
- Git tags: no local `v*` tags exist; `v0.0.0-beta.0` was not created.
- Trusted Publishing: repo workflow scaffolding exists for future OIDC publish
  work, but external npm trusted-publisher setup and beta patch execution are
  parked.
- Registry smoke: public `@beta` registry smoke passed in the release-hygiene
  lane; the Prompt 144 public-beta smoke also passed with package artifacts and
  mock-only provider behavior.

Parked release blockers:

- decide whether to leave or remediate `latest` pointing at the beta
- prove publish provenance before creating any `v0.0.0-beta.0` tag, or publish
  a clean beta patch instead
- complete external npm Trusted Publishing/OIDC package setup before any future
  registry publish

Resume instructions:

1. Start from `docs/release-hygiene-parked.md`.
2. Use `.github/workflows/npm-trusted-publish.yml` only after npm trusted
   publisher setup is verified.
3. Do not mutate dist-tags without explicit approval and
   `TYPAI_ALLOW_NPM_DIST_TAG_MUTATION=1`.
4. Do not create or push Git tags without proven provenance, explicit approval,
   and the required operator gate.

## Production Asset Lane

Lane status: **parked with host-provided-only fallback**.

Final asset state: **blocked / host-provided only**.

Current verified state:

- `packages/core/assets/production/MANIFEST.json` has
  `review.status: "blocked"`.
- `dictionary.mode: "production"` remains unavailable while the manifest is
  blocked.
- output package inclusion is `blocked`.
- no generated production dictionary binary exists in the repo or package
  tarballs.
- no raw ESDB/SCOWL, Hunspell, or Google Books Ngram source files are included
  in packages.
- host-provided Typai Dictionary Blob v1 bytes remain supported during
  `createTypaiCore()` initialization.
- scaled mock and fixture assets remain test-only and are not production
  coverage.

Current source and output status:

| Item | Status |
| --- | --- |
| ESDB/SCOWL dictionary source hash | pinned |
| Google Books Ngram `totalcounts-1` hash | pinned |
| 14 Google Ngram gzip partition hashes | missing |
| external pinned local source paths | missing |
| final package-visible production notices | placeholders only |
| generated output hash | absent |
| generated word count | absent |
| generated byte size | absent |
| spell-quality evidence against generated asset | absent |
| package dry-run proof for included asset | absent |
| final review signoff | blocked |

Resume instructions:

1. Start from `docs/production-asset-blocker-closure.md` and
   `docs/production-asset-execution-result.md`.
2. Provide external source paths outside the repo.
3. Compute and record the missing source hashes, especially all 14 Google Ngram
   gzip partitions.
4. Finalize package-visible license and attribution notices.
5. Update the manifest only when every source, hash, license, attribution,
   size, quality, package, and review gate has evidence.
6. Generate only after `review.status` is approved and package inclusion is not
   blocked.

## Core And Product Boundaries

The containment audit confirms these boundaries remain active:

- `@typai/core` remains local deterministic correction only.
- `@typai/core` does not import `@typai/completion-remote`.
- completion remains optional and separate.
- no browser API-key path exists.
- no real provider calls run in tests, demos, E2E, smoke, or CI.
- no next-edit logging exists.
- no local model inference exists.
- no real Codex adapter exists yet.
- valid-word autocorrect remains forbidden.
- protected-token writes remain zero.
- arbitrary delete-index autocorrect remains forbidden.
- accepted completions are not blue correction marks.

## Current Package And App State

The package set is usable for beta app work with the documented limits:

- local deterministic correction and editor adapters are available
- optional remote completion is available through mock/noop/endpoint providers
- endpoint provider credentials remain server-owned
- package smoke and public-beta smoke pass
- production dictionary coverage is not bundled and must not be claimed

Safe next work:

- Real Codex Adapter Foundation
- app dogfooding and product integration that preserves the existing
  correction/completion boundaries
- editor adapter bugfixes and UI work that do not change the production asset
  or registry state

Do not reopen release publishing or production asset generation unless a new
prompt explicitly selects that lane.

## Validation Summary

Final Prompt 144 gate results:

| Command | Result | Notes |
| --- | --- | --- |
| `pnpm test` | passed | Turbo reported 34 successful tasks. |
| `pnpm test:e2e` | passed | Chromium and Firefox suites passed; surface parity remained green. |
| `pnpm --filter @typai/core bench` | passed | Production mode remained blocked; scaled mock benchmark passed. |
| `pnpm bench:browser` | passed | Deterministic correction stayed below hard thresholds; mocked completion emitted warning-level rows only. |
| `pnpm bench:spell-quality` | passed | 142 cases; valid-word false autocorrect 0; protected-token false write 0; arbitrary delete-index autocorrect 0. |
| `pnpm bench:language-asset` | passed | Final rerun passed for blocked, built-in, host-provided, and scaled-mock modes. |
| `pnpm pack:dry` | passed | Core tarball excluded production and raw language assets. |
| `pnpm smoke:install` | passed | Local packed consumer smoke passed. |
| `pnpm smoke:public-beta` | passed | Package matrix, host-provided dictionary path, blocked production mode, and mock completion passed. |
| `pnpm scan:package-secrets` | passed | Seven release packages scanned clean. |
| `pnpm package:size-report` | passed | `@typai/core` packed size was 49.11 KiB; no production binary or raw source files included. |
| `pnpm release:check` | passed | Six public packages plus support package `@typai/ui` checked. |
| `pnpm docs:check` | passed | Required docs and consumer examples verified. |
| `pnpm dictionary:check-production` | passed | Correct blocked-mode output with no production asset bundled. |
| `pnpm dictionary:gate-status` | passed | Manifest blocked state and documented blockers reported. |
| `pnpm build` | passed | Existing demo/consumer Vite chunk-size warnings remain warning-level. |
| `pnpm lint` | passed | Biome completed with no fixes. |

## App-Continuation Decision

App work may proceed from this checkpoint.

Recommended next phase: **Real Codex Adapter Foundation**.

The release lane and production asset lane are both parked cleanly. They do not
block app work as long as future prompts preserve the documented boundaries and
do not publish, tag, mutate dist-tags, or generate production assets without
explicit approval.
