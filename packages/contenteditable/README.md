# @typai/contenteditable

Contenteditable adapter for Typai.

This package attaches local deterministic correction to a contenteditable
element and accepts an optional structural completion controller supplied by the
embedder. It does not include provider credentials, server examples, or direct
provider calls.

The package metadata uses `UNLICENSED` until the project license is selected.
No npm publish has occurred.
